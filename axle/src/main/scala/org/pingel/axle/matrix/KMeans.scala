package org.pingel.axle.matrix

class KMeans {

  import org.pingel.axle.matrix.DoubleJblasMatrixFactory._

  def cluster(K: Int, X: Matrix): Result = {
    
  }
  
  // X is NOT left-padded with 1's for k-means clustering
  
}